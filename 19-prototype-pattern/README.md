# Design Pattern: Prototype — Session Demo

| Demo | Run |
|---|---|
| [`order-template/`](order-template/README.md) | `cd order-template && ./run.sh` |

Third of nine design-pattern demos (17-25). A small, standalone example:
`Order` gains a `copy()` method, replacing manual, error-prone
field-by-field duplication. See the session-level note in
`17-singleton-pattern/README.md` for the `example/`/`exercise/` package
layout used across all nine.

Requires JDK 21+.
