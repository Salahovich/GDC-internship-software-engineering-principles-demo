# Design Pattern: Proxy — Session Demo

| Demo | Run |
|---|---|
| [`inventory-proxy/`](inventory-proxy/README.md) | `cd inventory-proxy && ./run.sh` |

Sixth of nine design-pattern demos (17-25). A small, standalone example:
a caching proxy stands in for a slow inventory lookup service, cutting
repeat calls down to one. See the session-level note in
`17-singleton-pattern/README.md` for the `example/`/`exercise/` package
layout used across all nine.

Requires JDK 21+.
