# Design Pattern: Chain of Responsibility — Session Demo

| Demo | Run |
|---|---|
| [`order-validation-chain/`](order-validation-chain/README.md) | `cd order-validation-chain && ./run.sh` |

Seventh of nine design-pattern demos (18-26), and the first of three
Behavioral patterns. A small, standalone example: order validation rules
move from one monolithic method into a chain of independent handlers.
See the session-level note in `18-singleton-pattern/README.md` for the
`example/`/`exercise/` package layout used across all nine.

Requires JDK 21+.
